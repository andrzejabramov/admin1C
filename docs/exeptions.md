### 📋 Таблица типовых исключений для модуля `backup`

| Категория ошибки             | Конкретная ситуация                            | Код ошибки / Признак      | Рекомендуемое действие в скрипте                                                    |
| ---------------------------- | ---------------------------------------------- | ------------------------- | ----------------------------------------------------------------------------------- |
| **Валидация входных данных** | Неверное имя ИБ (отсутствует в кластере)       | `ERR_IB_NOT_FOUND`        | Прервать обработку конкретной ИБ, продолжить остальные; вывести список доступных ИБ |
|                              | Имя ИБ с пробелами/спецсимволами без кавычек   | `ERR_INVALID_ARG`         | Отклонить запуск, показать пример корректного синтаксиса                            |
|                              | Конфликт флагов (`--ib` + `--all`)             | `ERR_FLAG_CONFLICT`       | Прервать выполнение, показать справку по приоритету флагов                          |
|                              | Неизвестный флаг / опечатка в флаге            | `ERR_UNKNOWN_FLAG`        | Прервать выполнение, предложить `--help`                                            |
| **Доступ к кластеру 1С**     | Кластер недоступен / остановлен                | `ERR_CLUSTER_UNREACHABLE` | Прервать весь бэкап-процесс, проверить статус `ragent`                              |
|                              | Нет прав у `usr1cv8` на управление ИБ          | `ERR_PERMISSION_DENIED`   | Прервать, предложить запуск через `sudo -u usr1cv8`                                 |
| **Файловая система**         | Нет места на диске (`/var/backups`)            | `ERR_NO_SPACE`            | Прервать текущую ИБ, проверить `df -h`, предложить очистку                          |
|                              | Нет прав на запись в `/var/backups/1c/`        | `ERR_WRITE_DENIED`        | Прервать, проверить владельца и права каталога                                      |
|                              | Прерывание записи (диск переполнен в процессе) | `ERR_WRITE_INTERRUPTED`   | Удалить неполный файл, зафиксировать ошибку                                         |
| **База данных (PostgreSQL)** | PostgreSQL недоступен                          | `ERR_PG_UNREACHABLE`      | Прервать бэкап ИБ, проверить `systemctl status postgresql`                          |
|                              | Ошибка `pg_dump` (повреждение БД)              | `ERR_PG_DUMP_FAILED`      | Зафиксировать код возврата `pg_dump`, сохранить лог                                 |
| **Сетевые операции**         | Таймаут при создании бэкапа ИБ через 1С        | `ERR_TIMEOUT`             | Прервать попытку, повторить (макс. 2 раза)                                          |
| **Рантайм**                  | Прерывание пользователем (`Ctrl+C`)            | `SIGINT`                  | Корректно завершить, удалить неполные файлы, вывести статистику                     |
