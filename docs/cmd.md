# Справочник команд ib_1c

Единый инструмент администрирования информационных баз 1С:Предприятие.

> **Важно:** Основной файл — `/opt/1cv8/scripts/admin1c.py`  
> Рекомендуемый вызов — через функцию `ib_1c` (настроена в `~/.bashrc`)

---

## 🔑 Быстрый старт

### Установка (выполняется один раз)

```bash
# Создать символическую ссылку
sudo ln -sf /opt/1cv8/scripts/admin1c.py /usr/local/bin/admin1c

# Добавить функцию в ~/.bashrc (если ещё не сделано)
echo 'ib_1c() { sudo admin1c "$@"; }' >> ~/.bashrc
source ~/.bashrc
```

### Первые шаги

```bash
# Посмотреть список всех ИБ в кластере
ib_1c list

# Добавить ИБ в управляемый список
ib_1c config --add artel_2025

# Создать бэкап всех управляемых ИБ
ib_1c backup --all --format dump

# Отправить бэкапы в облако
ib_1c cloud --all

# Очистить локальные бэкапы старше 3 дней
ib_1c prune --all --keep-days 3
```

---

## 🔑 Универсальные флаги безопасности

| Флаг        | Описание                                            | Пример                                      |
| ----------- | --------------------------------------------------- | ------------------------------------------- |
| `--dry-run` | Симуляция без изменений                             | `ib_1c prune --all --keep-days 3 --dry-run` |
| `--confirm` | Требует подтверждения перед деструктивной операцией | `ib_1c delete --ib test --confirm`          |
| `--verbose` | Подробный вывод                                     | `ib_1c backup --ib artel_2025 --verbose`    |
| `--help`    | Справка по подкоманде                               | `ib_1c backup --help`                       |

> ⚠️ Все деструктивные операции (`delete`, `restore`, `prune`, `rm`) требуют `--confirm` или `--dry-run`.

---

## 📋 Подкоманды

### `list` — список всех ИБ кластера

```bash
ib_1c list
```

Выводит таблицу со всеми ИБ, их статусом и размером.

---

### `info` — детали конкретной ИБ

```bash
ib_1c info --ib artel_2025
```

Показывает:

- Версию конфигурации
- Размер базы данных
- Путь к файлам ИБ
- Статус (работает/заблокирована)

---

### `backup` — создание резервной копии

```bash
# Одна ИБ
ib_1c backup --ib artel_2025 --format dump

# Несколько ИБ
ib_1c backup --ib artel_2025 oksana_2025 --format sql

# Все ИБ из списка
ib_1c backup --all --format dump

# Симуляция
ib_1c backup --ib artel_2025 --format dump --dry-run
```

**Форматы:**

- `dump` — бинарный формат `-Fc` (быстрое восстановление, ~1 ГБ)
- `sql` — текстовый дамп + `gzip` (анализ, перенос, ~1 ГБ)

> 💡 Бэкап выполняется через `pg_dump` на сервере БД → не блокирует ИБ.

---

### `restore` — восстановление из бэкапа _(в разработке)_

```bash
# Восстановить из конкретного бэкапа
ib_1c restore --ib artel_test --from 20260202_182603 --confirm

# Восстановить последний бэкап
ib_1c restore --ib artel_test --latest --confirm
```

> ⚠️ Требует `--confirm`. Создаёт новую ИБ (не перезаписывает существующую).

---

### `create` — создание новой ИБ _(в разработке)_

```bash
# Пустая ИБ
ib_1c create --name test_ib --empty --confirm

# Клонирование из шаблона
ib_1c create --name artel_test --template artel_2025 --confirm
```

---

### `delete` — удаление ИБ _(в разработке)_

```bash
ib_1c delete --ib test_ib --confirm
```

> ⚠️ Удаляет ИБ из кластера + все её локальные бэкапы. Требует `--confirm`.

---

### `config` — управление списком ИБ (`ib_list.conf`)

```bash
# Показать список
ib_1c config --list

# Добавить ИБ
ib_1c config --add artel_2025

# Удалить ИБ из списка (без удаления самой ИБ!)
ib_1c config --del test_ib

# Переименовать ИБ в списке
ib_1c config --mv artel_test artel_2026
```

> 💡 `ib_list.conf` содержит только имена ИБ — безопасен для коммита в Git.

---

### `cloud` — отправка бэкапов в облако _(в разработке)_

```bash
# Одна ИБ
ib_1c cloud --upload artel_2025

# Все ИБ
ib_1c cloud --all

# Симуляция
ib_1c cloud --all --dry-run
```

**Поддерживаемые облака:**

- Mail.ru Облако (через `rclone`)
- Yandex Disk (через `ydcmd`)

> 🔒 Настройки облака хранятся в `config/cloud_config.sh` (НЕ в Git!).

---

### `prune` — очистка локальных бэкапов _(в разработке)_

```bash
# Удалить бэкапы старше 3 дней для всех ИБ
ib_1c prune --all --keep-days 3

# Для одной ИБ
ib_1c prune --ib artel_2025 --keep-days 3

# Симуляция
ib_1c prune --all --keep-days 3 --dry-run
```

> 💡 Рекомендуемый период: **3 дня** (диск 100 ГБ, 14 ИБ × ~1.75 ГБ = ~24.5 ГБ/день).

---

### `rm` — ручное удаление локальных бэкапов _(в разработке)_

Удаляет бэкапы **только локально**, без взаимодействия с облаком.

```bash
# Удалить ВСЕ бэкапы одной ИБ
ib_1c rm --ib artel_2025 --confirm

# Удалить конкретный бэкап по временной метке
ib_1c rm --ib artel_2025 --timestamp 20260202_182603 --confirm

# Удалить бэкапы старше указанной даты
ib_1c rm --ib artel_2025 --older-than 20260201 --confirm

# Симуляция удаления
ib_1c rm --ib artel_2025 --dry-run
```

> ⚠️ **Важно:** Эта команда **не удаляет ИБ из кластера 1С** — только её бэкапы из `/var/backups/1c`.  
> ⚠️ Требует `--confirm` для защиты от случайного удаления.

---

### `storage` — просмотр хранилища бэкапов _(в разработке)_

```bash
# Обзор всего хранилища
ib_1c storage

# Статистика по конкретной ИБ
ib_1c storage --ib artel_2025
```

**Вывод включает:**

- Свободное/занятое место на диске `/var/backups/1c`
- Количество бэкапов по каждой ИБ
- Список последних бэкапов с датами и размерами
- Прогноз дней хранения при текущем темпе роста

**Пример вывода:**

```
📁 Хранилище бэкапов: /var/backups/1c
┌──────────────┬──────────┬──────────┬──────────┐
│ Диск         │ Всего    │ Занято   │ Свободно │
├──────────────┼──────────┼──────────┼──────────┤
│ /dev/vdb     │ 98 ГБ    │ 28 ГБ    │ 70 ГБ    │
└──────────────┴──────────┴──────────┴──────────┘

📊 Статистика по ИБ:
┌──────────────────────┬─────────┬──────────┬──────────────┐
│ ИБ                   │ Бэкапов │ Последний│ Размер       │
├──────────────────────┼─────────┼──────────┼──────────────┤
│ artel_2025           │ 5       │ 02.02    │ 991M         │
│ oksana_2025          │ 3       │ 02.02    │ 3.8G         │
│ resug_2025           │ 2       │ 02.02    │ 1.3G         │
│ IP_Smirnov_V_S_2025  │ 1       │ 02.02    │ 892M         │
└──────────────────────┴─────────┴──────────┴──────────────┘

📈 Прогноз: при текущем темпе (~24.5 ГБ/день) свободного места хватит на ~2.8 дня
```

---

### `check` — проверка целостности ИБ _(в разработке)_

```bash
ib_1c check --ib artel_2025
```

Выполняет:

- Проверку подключения к ИБ
- Тестовый запрос к СУБД
- Валидацию последнего бэкапа

---

## 🔐 Структура файлов и безопасность

```
/opt/1cv8/scripts/
├── admin1c.py               ← ЕДИНСТВЕННЫЙ файл (точка входа)
├── ib_list.conf             ← Список ИБ (в Git ✅)
├── engines/
│   ├── backup.sh            ← Бэкап (уже работает)
│   ├── prune.sh             ← Очистка локальных бэкапов
│   └── cloud_upload.sh      ← Загрузка в облако
└── config/
    ├── db_config.sh         ← Подключение к БД (НЕ в Git 🔒)
    └── cloud_config.sh      ← Настройки облака (НЕ в Git 🔒)
```

### `.gitignore`

```gitignore
# Секреты и временные файлы
config/*.sh
*.log
*.bak
*.tmp
lost+found/
```

---

## 🔄 Типичные рабочие процессы

| Сценарий                            | Команды                                                                                                                                             |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Ежедневный бэкап**                | `ib_1c backup --all --format dump`<br>`ib_1c cloud --all`<br>`ib_1c prune --all --keep-days 3`                                                      |
| **Создание тестовой копии**         | `ib_1c create --name artel_test --template artel_2025 --confirm`<br>`ib_1c config --add artel_test`<br>`ib_1c backup --ib artel_test --format dump` |
| **Очистка перед увеличением диска** | `ib_1c storage`<br>`ib_1c rm --ib test_ib --confirm`<br>`ib_1c prune --all --keep-days 1 --confirm`                                                 |
| **Экстренное освобождение места**   | `ib_1c storage --ib oksana_2025`<br>`ib_1c rm --ib oksana_2025 --older-than 20260201 --confirm`                                                     |

---

## 📊 Полная таблица подкоманд (обновлённая)

| Подкоманда | Статус         | Описание                              | Ключевые флаги                                                  |
| ---------- | -------------- | ------------------------------------- | --------------------------------------------------------------- |
| `list`     | ✅ Готово      | Список всех ИБ кластера               | —                                                               |
| `info`     | 🔵 Планируется | Детали конкретной ИБ                  | `--ib`                                                          |
| `backup`   | ✅ Готово      | Создание резервной копии              | `--ib`, `--all`, `--format`, `--dry-run`                        |
| `restore`  | 🔵 Планируется | Восстановление из бэкапа              | `--ib`, `--from`, `--latest`, `--confirm`                       |
| `create`   | 🔵 Планируется | Создание новой ИБ                     | `--name`, `--template`, `--empty`, `--confirm`                  |
| `delete`   | 🔵 Планируется | Удаление ИБ из кластера               | `--ib`, `--confirm`                                             |
| `config`   | 🟡 В работе    | Управление `ib_list.conf`             | `--add`, `--del`, `--mv`, `--list`                              |
| `cloud`    | 🔵 Планируется | Отправка в облако                     | `--upload`, `--all`, `--dry-run`                                |
| `prune`    | 🔵 Планируется | Автоматическая очистка старых бэкапов | `--ib`, `--all`, `--keep-days`, `--dry-run`                     |
| `rm`       | 🔵 Планируется | Ручное удаление локальных бэкапов     | `--ib`, `--timestamp`, `--older-than`, `--confirm`, `--dry-run` |
| `storage`  | 🔵 Планируется | Просмотр хранилища бэкапов            | `--ib`                                                          |
| `check`    | 🔵 Планируется | Проверка целостности ИБ               | `--ib`, `--all`                                                 |

❗ ВАЖНО. Порядок указания флагов после подкоманды значения НЕ имеет!

## 💡 Зачем нужны обе команды очистки (`prune` vs `rm`)

| Команда | Назначение                         | Автоматизация     | Типичное использование                                   |
| ------- | ---------------------------------- | ----------------- | -------------------------------------------------------- |
| `prune` | Автоматическая ротация по возрасту | ✅ Для `cron`     | Ежедневная очистка старше 3 дней                         |
| `rm`    | Ручное управление бэкапами         | ❌ Только вручную | Экстренное освобождение места, удаление тестовых бэкапов |

---

## 💡 Лучшие практики

1. **Всегда используй `--dry-run`** перед деструктивными операциями
2. **Храни бэкапы 3 дня локально + в облаке** — баланс места и надёжности
3. **Добавляй только рабочие ИБ в `ib_list.conf`** — тестовые удаляй после использования
4. **Проверяй свободное место** перед массовым бэкапом:
   ```bash
   df -h /var/backups/1c
   ```
5. **Не прерывай бэкап вручную** — дождись завершения или используй `--dry-run`

---

## 🚀 Следующие шаги разработки

| Этап                  | Статус         | Команды            |
| --------------------- | -------------- | ------------------ |
| Бэкап + прогресс      | ✅ Готово      | `backup`, `prune`  |
| Управление списком ИБ | 🟡 В работе    | `config`           |
| Загрузка в облако     | 🔵 Планируется | `cloud`            |
| Восстановление        | 🔵 Планируется | `restore`          |
| Создание/удаление ИБ  | 🔵 Планируется | `create`, `delete` |
| Проверка целостности  | 🔵 Планируется | `check`            |

---

> 📌 **Версия документа:** 1.0 (февраль 2026)  
> **Автор:** Андрей (@andrey) + AI-ассистент  
> **Цель:** Единый, безопасный и интуитивный интерфейс для администрирования 1С
