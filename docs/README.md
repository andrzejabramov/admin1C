# 🖥️ 1C Server Admin Toolkit

Сервис управления сервером 1С:Предприятие 8.3 под Linux (Ubuntu 18.04), разработанный с применением ИИ-ассистента **Qwen3-Max**. Предоставляет единый интерфейс для автоматизации рутинных административных задач через гибридную архитектуру **Python (управление) + Bash (низкоуровневые операции)**.

---

## 📌 Основные цели

- Упрощение ежедневного администрирования кластера 1С
- Предотвращение человеческих ошибок через автоматизацию
- Централизованное управление несколькими информационными базами
- Сохранение прозрачности операций (логирование, откаты)

---

## ⚙️ Технические требования

| Компонент      | Версия / Требование                  |
| -------------- | ------------------------------------ |
| ОС             | Ubuntu 18.04 LTS                     |
| 1С:Предприятие | 8.3.27.1989                          |
| Python         | 3.11+ (venv)                         |
| PostgreSQL     | 11+ (для бэкапов через `pg_dump`)    |
| Дополнительно  | `rclone`, `certbot`, `rac` в `$PATH` |

---

## 🗂️ Структура проекта

/opt/1cv8/scripts/  
├── admin1c.py # Единая точка входа (CLI-интерфейс)  
├── requirements.txt # Зависимости Python  
├── .version # Версия системы (интегрируется во все скрипты)  
├── ib_list.conf # Список управляемых ИБ (по одной на строку)  
├── .gitignore # Правила игнорирования для Git
│ .
├── lib/ # Python-модули (логика управления)  
│ ├── init.py # Инициализация пакета  
│ ├── config.py # Чтение .version, ib_list.conf, пути  
│ ├── cluster.py # Работа с кластером через rac/ras (JSON parsing)  
│ └── utils.py # Утилиты: trim строк, логирование, безопасные вызовы  
│  
├── engines/ # Bash-движки (низкоуровневые операции)  
│ ├── backup_dump.sh # Бэкап в формате .dump (pg_dump)  
│ ├── backup_sql.sh # Бэкап в формате .sql.gz (сжатый дамп)  
│ ├── cleanup.sh # Очистка зависших сессий  
│ └── cloud_upload.sh # Выгрузка в облако (mail.ru через rclone)  
│  
└── docs/ # Документация  
├── README.md # ← Этот файл (обзор проекта)  
├── ssl.md # [1] Управление SSL-сертификатами  
├── sessions.md # [2] Закрытие неактивных сессий  
├── ib_lifecycle.md # [3] Создание и удаление ИБ  
├── backup.md # [4] Резервное копирование (.dump / .sql.gz)  
├── config_update.md # [5] Обновление конфигураций ИБ  
├── rac_reference.md # Справочник команд rac/ras  
└── architecture.md # Детали гибридной архитектуры Python+Bash

> 💡 **Примечание:** В текущей версии используются отдельные скрипты `backup_dump.sh` и `backup_sql.sh`.
> Плановая архитектура предполагает объединение в единый `backup.sh` с параметром `--format`.

---/opt/1cv8/scripts/
├── admin1c.py # Единая точка входа (CLI-интерфейс)
├── requirements.txt # Зависимости Python
├── .version # Версия системы (интегрируется во все скрипты)
├── ib_list.conf # Список управляемых ИБ (по одной на строку)
├── .gitignore # Правила игнорирования для Git
│
├── lib/ # Python-модули (логика управления)
│ ├── init.py # Инициализация пакета
│ ├── config.py # Чтение .version, ib_list.conf, пути
│ ├── cluster.py # Работа с кластером через rac/ras (JSON parsing)
│ └── utils.py # Утилиты: trim строк, логирование, безопасные вызовы
│
├── engines/ # Bash-движки (низкоуровневые операции)
│ ├── backup_dump.sh # Бэкап в формате .dump (pg_dump)
│ ├── backup_sql.sh # Бэкап в формате .sql.gz (сжатый дамп)
│ ├── cleanup.sh # Очистка зависших сессий
│ └── cloud_upload.sh # Выгрузка в облако (mail.ru через rclone)
│
└── docs/ # Документация
├── README.md # ← Этот файл (обзор проекта)
├── ssl.md # [1] Управление SSL-сертификатами
├── sessions.md # [2] Закрытие неактивных сессий
├── ib_lifecycle.md # [3] Создание и удаление ИБ
├── backup.md # [4] Резервное копирование (.dump / .sql.gz)
├── config_update.md # [5] Обновление конфигураций ИБ
├── rac_reference.md # Справочник команд rac/ras
└── architecture.md # Детали гибридной архитектуры Python+Bash

## 🚀 Основной функционал

| №   | Функция                                     | Описание                                                                            | Детали                                                     |
| --- | ------------------------------------------- | ----------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| [1] | [Управление SSL](ssl.md)                    | Автоматическое обнаружение и продление сертификатов Certbot для веб-публикаций ИБ   | Проверка срока действия, безопасная перезагрузка Apache    |
| [2] | [Закрытие сессий](sessions.md)              | Поиск и принудительное завершение зависших/неактивных сессий пользователей          | Настройка порога бездействия (по умолчанию: 30 мин)        |
| [3] | [Жизненный цикл ИБ](ib_lifecycle.md)        | Создание тестовых ИБ, клонирование, удаление через `rac infobase`                   | Шаблоны конфигурации, безопасное удаление с подтверждением |
| [4] | [Резервное копирование](backup.md)          | Единый интерфейс для бэкапов в форматах `.dump` (pg_dump) и `.sql.gz` (сжатый дамп) | Выбор ИБ из `ib_list.conf` или ручной ввод, логирование    |
| [5] | [Обновление конфигураций](config_update.md) | Загрузка обновлений из файлов `.cf` в существующие ИБ                               | Поддержка режима отладки, откат при ошибках                |

> 💡 Все операции выполняются от имени пользователя `usr1cv8` (владельца кластера 1С).

---

## 🧪 Быстрый старт

```bash
# 1. Активируем окружение
cd /opt/1cv8/scripts
source venv/bin/activate

# 2. Просмотр справки
python admin1c.py --help

# 3. Примеры использования
python admin1c.py backup --format dump --ib artel_2025
python admin1c.py sessions cleanup --timeout 30
python admin1c.py ssl renew --dry-run
```

🔒 Безопасность  
Все чувствительные данные (пароли, ключи) исключены из Git через .gitignore  
Bash-движки работают с минимальными правами (usr1cv8)  
Критические операции (удаление ИБ) требуют подтверждения  
Все действия логируются в /var/log/1c-admin/  
📜 Лицензия  
Проект распространяется «как есть» без гарантий. Использование на свой страх и риск.  
Рекомендуется тестирование в изолированной среде перед применением в продакшене.  
💡 Совет: Для расширения функционала создавайте новые .md-файлы в /docs/ и регистрируйте их в этом README.md. Это обеспечит единообразие документации.
